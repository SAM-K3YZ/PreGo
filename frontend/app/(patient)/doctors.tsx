import { View, Text } from 'react-native';

export default function Screen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>doctors screen — build from Stitch prompt in Notion Phase 10</Text>
    </View>
  );
}
